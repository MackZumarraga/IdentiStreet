import React from 'react';

export default () => (
  <div>
    <div>list of ListIndexItem/s each with favorite listing button (heart turns red when clicked if logged in/ if not logged in, redirects to login signup modal)</div>
    <div>SaveSearch component button (renders a modal to confirm saved search if logged in/ if not logged in, redirects to login signup modal)</div>
    <div>MapView rendering marker on the map</div>
  </div>
)